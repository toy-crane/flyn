/**
 * 표현 노트가 쓰는 말.
 *
 * 담긴 영어와 한국어 뜻, 인물 이름, 스토리 제목은 여기에 없다. 그것은 서버가
 * 들고 있고 앱은 받은 글을 그대로 보여 준다.
 */
export const noteLabels = {
  /** 아직 담은 것이 없는 노트. 여기서 할 일이 없으므로 버튼을 두지 않는다. */
  emptyTitle: "아직 저장한 표현이 없어요.",
  /** 카드를 밀면 드러나는 하나뿐인 동작. */
  erase: "삭제",
  /** 인물 대사가 아닌 카드의 출처. 내가 쓴 말에서 온 표현이다. */
  mine: "내 표현",
  /** 출처 표시 뒤에 붙는 자리. 어느 스토리의 몇 화에서 담았는지. */
  source: (storyTitle: string, episodeNumber: number) =>
    `${storyTitle} ${episodeNumber}화`,
  /** 하단 탭의 이름. 화면 제목과 다르다. */
  tab: "노트",
  /** 화면의 큰 제목. */
  title: "표현 노트",
  /** 목록을 읽어 오지 못했을 때. */
  unavailable: "표현 노트를 불러오지 못했어요.",
} as const;

{
  "name": "@repo/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "ultracite check .",
    "check-types": "tsc --noEmit",
    "dev": "bun run --cwd ../.. env:setup && BUN_PORT=3901 bun run --hot src/index.ts",
    "eval:ask": "bun run --cwd ../.. env:setup && bun eval/ask.ts",
    "eval:correction": "bun run --cwd ../.. env:setup && bun eval/correction.ts",
    "eval:multi-cast": "bun run --cwd ../.. env:setup && bun eval/multi-cast.ts",
    "eval:role-ownership": "bun run --cwd ../.. env:setup && bun eval/role-ownership.ts",
    "eval:scene": "bun run --cwd ../.. env:setup && bun eval/scene.ts",
    "eval:story": "bun run --cwd ../.. env:setup && bun eval/story-creation.ts",
    "eval:story-play": "bun run --cwd ../.. env:setup && bun eval/story-play.ts",
    "fix": "ultracite fix .",
    "test": "bun test src eval",
    "test:integration": "bun test integration"
  },
  "dependencies": {
    "@repo/supabase": "workspace:*",
    "@supabase/server": "1.4.1",
    "@supabase/supabase-js": "2.112.2",
    "@vercel/functions": "3.9.7",
    "ai": "7.0.58",
    "blurhash": "2.0.5",
    "fast-png": "6.2.0",
    "hono": "4.13.1"
  },
  "devDependencies": {
    "@ai-sdk/provider": "4.0.7",
    "@types/bun": "1.3.14",
    "@types/node": "26.1.2",
    "typescript": "5.9.2"
  }
}

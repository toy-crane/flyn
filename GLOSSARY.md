# Glossary

flyn은 AI와 대화한 기록을 채팅방 단위로 보관하는 iOS 앱과 그 백엔드다. 이
문서는 제품과 저장소에서 **실제로 뜻이 갈릴 수 있는 말**만 정의한다.

## 인증

**세션**:
로그인한 사용자를 나타내는 인증 세션(access token + refresh token). 작업
단위를 가리킬 때는 이 말을 쓰지 않는다 — 그것은 **태스크**다.
_Avoid_: 로그인 상태, 로그인 정보

**이메일 OTP**:
이메일로 받은 **6자리 일회용 코드**로 로그인하는 경로. 링크가 아니다 —
매직링크는 기각됐고 같은 것을 가리키지 않는다.
_Avoid_: 매직링크, 인증번호, 이메일 링크

**자동 검증**:
사람 손 없이 재현되는 검증. 이 저장소에서 로그인 이후를 자동 검증하는 경로는
**이메일 OTP 하나뿐**이다. 소셜 로그인은 여기 들어올 수 없고, 사람이 눌러
확인하는 것은 자동 검증이 아니다.
_Avoid_: E2E, 통합 테스트

## 환경·빌드

**dev build**:
Expo Go 대신 쓰는, 네이티브 모듈이 포함된 개발용 앱 빌드. 네이티브 모듈이나
config plugin이 바뀌면 다시 빌드해야 한다.
_Avoid_: 개발 빌드, 커스텀 빌드, 로컬 빌드

**앱 테마**:
앱이 소유하며 RN과 네이티브 경계에서 공통으로 쓰는 **제품 의미 색 역할**과
시스템 밝기 변화에 반응하는 규칙. iOS `Form`·`List`·sheet·navigation의 기본
표면과 재질은 포함하지 않고, 재사용 컴포넌트 세트도 뜻하지 않는다 — 그것은
디자인 시스템이다.
_Avoid_: 색상표, 디자인 시스템, 스타일 모음

**네이티브 표면**:
iOS가 의미를 이미 아는 `Form`·`List`·sheet·navigation·alert의 기본 배경, 재질,
위계와 상호작용. RN 화면과 픽셀 단위로 같은 색이어야 하는 표면이 아니며, 제품
상태 색만으로 다시 칠하지 않는다.
_Avoid_: 앱 배경, 네이티브 색, Universal 테마

**`supabase/`**:
CLI가 읽는 프로젝트 폴더 — `config.toml`, 선언적 스키마, 마이그레이션, 메일
템플릿. 워크스페이스가 아니고 루트에 있어야 한다. 앱과 API가 `import`하는 생성
타입 패키지 **`packages/supabase`와 다른 것**을 가리킨다.
_Avoid_: supabase 패키지, DB 워크스페이스

## 작업 문서

**결정 계약**:
사람이 승인한 현재 선택과 경계. [docs/decisions](docs/decisions/README.md)에 주제당
한 파일만 두고, 같은 주제가 바뀌면 그 파일을 고친다. 과거는 Git에만 남긴다.
_Avoid_: ADR, 결정 기록, 방침, 정책

**가정**:
반증이 나오면 계약 변경 없이 뒤집는 기본값. 결정 계약과 같은 무게로 읽지 않는다.
_Avoid_: 전제, 임시 결정

**태스크**:
한 작업 세션 안에 끝나도록 자른 작업 단위. 끝이 곧 검증 가능 지점이다.
_Avoid_: 세션(작업 단위를 가리킬 때), 이슈, 스텝

## 제품

**채팅방**:
한 사용자가 AI와 주고받은 메시지를 시간순으로 묶어 보관하는 대화 단위.
_Avoid_: 세션, 스레드, 대화방

**메시지**:
채팅방 안에 저장되는 사용자 발화 또는 AI 응답 하나. 스트리밍 중 화면에만
보이는 조각은 별도 메시지가 아니다.
_Avoid_: 프롬프트, 답변, 말풍선

**프로필**:
인증 사용자 한 명을 앱 안에서 나타내는 계정 정보. 이메일은 본인만 보고, 닉네임과
아이디는 남에게 보인다. 사용자가 직접 정하는 값은 그 둘뿐이다.
_Avoid_: 공개 프로필, 사용자 카드

**닉네임**:
사람이 읽으라고 있는 이름. 어느 문자든 쓸 수 있고 남과 겹쳐도 되지만,
**이모지와 장식용 기호는 이름이 아니라서 막는다.** 로그인 provider가 준 이름을
후보로 채우지만, 정하는 것은 사용자다.
_Avoid_: 표시 이름, 이름, 아이디

**아이디**:
남이 나를 찾아 입력하는 유일한 키. 영문 소문자·숫자·밑줄·마침표만 쓰고 겹칠 수
없다. **이것으로 로그인하지 않는다** — flyn의 로그인은 Apple·Google·이메일
OTP뿐이다.
_Avoid_: 유저네임, 사용자 이름, 닉네임, 핸들

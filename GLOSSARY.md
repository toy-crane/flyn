# Glossary

flyn은 재미 중심 스토리 롤플레이로 영어 라이팅을 연습하는 iOS 앱과 그
백엔드다. 이 문서는 제품과 저장소에서 **실제로 뜻이 갈릴 수 있는 말**만
정의한다.

## 인증

**세션**:
로그인한 사용자를 나타내는 인증 세션(access token + refresh token). 작업
단위를 가리킬 때는 이 말을 쓰지 않는다 — 그것은 **태스크**다.
_Avoid_: 로그인 상태, 로그인 정보

**이메일 OTP**:
이메일로 받은 **6자리 일회용 코드**로 로그인하는 경로. 링크가 아니다 —
매직링크는 기각됐고 같은 것을 가리키지 않는다.
_Avoid_: 매직링크, 인증번호, 이메일 링크

**자동 검증**:
사람 손 없이 재현되는 검증. 이 저장소에서 로그인 이후를 자동 검증하는 경로는
**이메일 OTP 하나뿐**이다. 소셜 로그인은 여기 들어올 수 없고, 사람이 눌러
확인하는 것은 자동 검증이 아니다.
_Avoid_: E2E, 통합 테스트

## 환경·빌드

**dev build**:
Expo Go 대신 쓰는, 네이티브 모듈이 포함된 개발용 앱 빌드. 네이티브 모듈이나
config plugin이 바뀌면 다시 빌드해야 한다.
_Avoid_: 개발 빌드, 커스텀 빌드, 로컬 빌드

**앱 테마**:
앱이 소유하며 전체에서 공통으로 쓰는 시각적 의미 역할과 시스템 밝기 변화에
반응하는 규칙. 재사용 컴포넌트 세트를 뜻하지 않는다 — 그것은 디자인 시스템이다.
_Avoid_: 색상표, 디자인 시스템, 스타일 모음

**`supabase/`**:
CLI가 읽는 프로젝트 폴더 — `config.toml`, 선언적 스키마, 마이그레이션, 메일
템플릿. 워크스페이스가 아니고 루트에 있어야 한다. 앱과 API가 `import`하는 생성
타입 패키지 **`packages/supabase`와 다른 것**을 가리킨다.
_Avoid_: supabase 패키지, DB 워크스페이스

## 작업 문서

**확정 결정**:
뒤집으려면 새 결정 기록이 필요한 위치. [docs/decisions](docs/decisions/README.md)에
한 줄씩 서 있다.
_Avoid_: 방침, 정책

**가정**:
반증이 나오면 기록 없이 뒤집는 기본값. 확정 결정과 같은 무게로 읽으면 안 된다.
_Avoid_: 전제, 임시 결정

**태스크**:
한 작업 세션 안에 끝나도록 자른 작업 단위. 끝이 곧 검증 가능 지점이다.
_Avoid_: 세션(작업 단위를 가리킬 때), 이슈, 스텝

## 제품

**에피소드**:
롤플레이 한 판을 묶는 단위 — 스토리, 상대 역할, 내 역할, 목표 3개, 그 대화와
첨삭·총평까지 전부. 위저드에서 단계별로 골라 만든다.
_Avoid_: 시나리오, 세션, 스토리(에피소드 전체를 가리킬 때)

**스토리**:
에피소드의 상황 설정. 위저드 첫 단계에서 고르는 구성 요소이지, 에피소드
전체가 아니다.
_Avoid_: 배경, 시나리오

**관심 분야**:
온보딩에서 고르는 취향 — 스토리를 끌어낼 **재미 기준의 소재**다. 면접 연습
같은 유틸리티 카테고리가 아니다.
_Avoid_: 카테고리, 학습 목적

**목표**:
에피소드마다 AI가 세우는 달성 조건 3개. 턴마다 판정해 체크하고, 다 채워지면
대화가 마무리 국면으로 간다.
_Avoid_: 미션, 퀘스트

**첨삭**:
유저 버블 하나에 붙는 학습 피드백. 상태는 셋뿐이다 — 교정(오류), 통과(완벽,
아이콘만), 표현 제시(한글 버블에 영어 표현 제안). AI 버블에는 붙지 않는다.
_Avoid_: 피드백, 코멘트, 교정(첨삭 전체를 가리킬 때)

**해설 챗**:
첨삭 1건에 1:1로 붙어 저장되는 과외 대화. 여기서는 한국어로 질문해도 된다.
에피소드 기록에 섞이지 않는다.
_Avoid_: 추가 챗, 스레드, 서브 챗

**총평**:
에피소드가 어떤 경로로 끝나든 나오는 마무리 요약 — 배운 표현, 주요 교정, 목표
결과. 홈 카드의 요약 한 줄이 여기서 나온다.
_Avoid_: 리포트, 학습 결과

**채팅방**:
한 사용자가 AI와 주고받은 메시지를 시간순으로 묶어 보관하는 대화 단위. 지금은
에피소드 대화와 해설 챗 두 종류가 있다.
_Avoid_: 세션, 스레드, 대화방

**메시지**:
채팅방 안에 저장되는 사용자 발화 또는 AI 응답 하나. 스트리밍 중 화면에만
보이는 조각은 별도 메시지가 아니다.
_Avoid_: 프롬프트, 답변, 말풍선

**프로필**:
인증 사용자 한 명을 앱 안에서 나타내는 **비공개 계정 정보**. 사용자가 확인하는
이메일과 직접 정하는 표시 이름을 담는다. 다른 사용자에게 노출하는 공개 프로필을
가리키지 않는다.
_Avoid_: 공개 프로필, 사용자 카드
